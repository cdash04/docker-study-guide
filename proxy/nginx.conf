http {
    upstream api {
        server api:3000;
    }

    server {
        listen 80;

        location /health {
            proxy_pass http://api;

        }

        location /api {
            proxy_pass http://api;
        }
    }
}

events {

}