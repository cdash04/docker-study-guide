global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: express
    static_configs:
      - targets:
          - api:3000
  - job_name: postgres
    static_configs:
      - targets:
          - postgres-exporter:9187
